<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="IV_game.Presentation.Views.ResultView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="16" MaxWidth="700" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="ResultHeading"
                   Grid.Row="0"
                   FontSize="28"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap" />

        <TextBlock x:Name="ResultGrade"
                   Grid.Row="1"
                   FontSize="18"
                   TextWrapping="Wrap" />

        <Grid Grid.Row="2" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="2">
                <TextBlock Text="Poäng" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="ScoreValue" FontSize="24" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Grid.Column="1" Spacing="2">
                <TextBlock Text="Rätta svar" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="CorrectValue" FontSize="24" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Grid.Column="2" Spacing="2">
                <TextBlock Text="Kritiska fel" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="CriticalValue" FontSize="24" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Grid.Column="3" Spacing="2">
                <TextBlock Text="Andel rätt" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="PercentValue" FontSize="24" FontWeight="SemiBold" />
            </StackPanel>
        </Grid>

        <ProgressBar x:Name="ResultProgress" Grid.Row="3" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="12">
            <Button Content="Öva igen (nytt patientfall)"
                    Style="{ThemeResource AccentButtonStyle}"
                    Click="OnRestartClick" />
            <Button Content="Topplista"
                    Click="OnHighScoresClick" />
        </StackPanel>
    </Grid>
</UserControl>
