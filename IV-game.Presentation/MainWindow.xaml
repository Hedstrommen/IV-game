<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="IV_game.Presentation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PVK-spelet – Lär dig lägga in perifer venkateter">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock x:Name="HeaderText"
                       Text="PVK-spelet"
                       FontSize="28"
                       FontWeight="SemiBold" />
            <TextBlock x:Name="SubHeaderText"
                       Text="En simuleringsövning enligt Vårdhandboken – perifer venkateter"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap" />
        </StackPanel>

        <ContentControl x:Name="MainContent" Grid.Row="2" />

    </Grid>
</Window>
